# Phase 4 — Google Photos

Status: **verified (offline-safe)**

Optional. Scope `photoslibrary.readonly`. Persist media IDs only. Incremental `SyncState` cursor. App safety budgets. No GPS-radius search.
