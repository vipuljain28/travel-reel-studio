# Phase 5 — Places

Status: **verified**

`POST /api/v1/places/resolve` caches by normalized query. Places API (New) text search with field mask id/displayName/formattedAddress/location. Local fallback when disabled.
