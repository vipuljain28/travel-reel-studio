# Google Photos

Phase 4. Library API quotas are documented by Google (~10k requests/day, ~75k media-byte requests/day). Store `providerMediaId`, never permanent `baseUrl`. Incremental sync only. App budget via `GOOGLE_PHOTOS_DAILY_REQUEST_BUDGET`.
